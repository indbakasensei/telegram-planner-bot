# BAKA dependencies — audited v14.12 (see CHANGELOG.md).
#
# Direct dependencies are marked; everything else is a pinned transitive
# dependency of one of them (verified against installed metadata: every
# entry below is reachable from a direct dependency's requirement chain,
# and every removed package was reachable from none).
#
# Removed in the v14.12 audit (never imported, no reverse-dependency):
#   anthropic; the entire unused Google/gRPC stack
#   (google-ai-generativelanguage, google-api-core,
#   google-api-python-client, google-auth, google-auth-httplib2,
#   googleapis-common-protos, grpcio, grpcio-status, proto-plus,
#   protobuf, pyasn1, pyasn1_modules, httplib2, uritemplate, pyparsing);
#   requests (+ charset-normalizer, urllib3); cryptography (+ cffi,
#   pycparser — PTB's optional [passport] extra, unused); tenacity;
#   websockets; docstring_parser.
#
# ⚠ httpx MUST stay pinned to 0.25.2 — newer versions break
#   python-telegram-bot 20.7 (see CLAUDE.md).

# ── Direct dependencies ──
python-telegram-bot==20.7
openai==2.41.0
python-dotenv==1.2.2
APScheduler==3.10.4
pytz==2026.2
httpx==0.25.2
pytest==9.1.1
pytest-asyncio==1.4.0

# ── Pinned transitive dependencies ──
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.5.20
distro==1.9.0
h11==0.16.0
httpcore==1.0.9
idna==3.18
iniconfig==2.3.0
jiter==0.15.0
pluggy==1.6.0
Pygments==2.20.0
pydantic==2.13.4
pydantic_core==2.46.4
six==1.17.0
sniffio==1.3.1
tqdm==4.68.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzlocal==5.3.1
